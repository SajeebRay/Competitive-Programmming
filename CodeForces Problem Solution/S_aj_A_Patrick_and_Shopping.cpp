#include<iostream>

using namespace std;

int main()
{
    long long a, b, c, resul;
    cin >> a >> b >> c;

    if (a+b >= c )
        resul = a+b+c;
    else if ()
        resul = 2*a + 2*b;
    cout << resul;

    return 0;
}
