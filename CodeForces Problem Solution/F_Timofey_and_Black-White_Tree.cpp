#include<bits/stdc++.h>
#define ll long long 
using namespace std;
const ll maxn = 200005;
bool vis[maxn];
vector<ll> adj[maxn];
ll ans = 1e5, cnt;
vector<ll> col, vec;

void dfs(ll sv){
  vis[sv] = true;
  
}

